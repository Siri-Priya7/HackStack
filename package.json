{
  "name": "voice-inventory-management",
  "version": "1.0.0",
  "description": "Voice-Based Inventory Management for Small Indian Businesses",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node backend/src/server.js",
    "start:dev": "concurrently \"npm start --prefix backend\" \"npm run preview --prefix frontend\"",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "test": "node inventory-engine/test/unitTest.js && node voice-ai/test/voiceTest.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.8.1",
    "concurrently": "^9.1.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "keywords": [
    "voice-inventory",
    "kirana",
    "hindi",
    "hinglish",
    "trade-units",
    "hackathon"
  ],
  "author": "Hackathon Team",
  "license": "MIT"
}
